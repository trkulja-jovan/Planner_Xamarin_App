<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:PlannerMob.ViewModels"
             x:Class="PlannerMob.Pages.AttendeeView"
             Title="Lista ucesnika">
    <ContentPage.BindingContext>
        <vm:AttendeeViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <CollectionView ItemsSource="{Binding Attendees}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Image Source="{Binding ImageUrl}"
                                   WidthRequest="90" HeightRequest="90"
                                   Grid.Column="0"/>

                            <StackLayout Orientation="Vertical" Grid.Column="1">
                                <Label Text="{Binding Name}" FontSize="Title"/>
                                <Label Text="{Binding Surname}" FontSize="Title"/>
                            </StackLayout>
                            <StackLayout Orientation="Vertical" Grid.Column="2">
                                <Label Text="{Binding Action}" FontSize="Title"/>
                            </StackLayout>
                            <StackLayout Orientation="Vertical" Grid.Column="3">
                                <Label Text="{Binding TimeStamp}" FontSize="Title"/>
                            </StackLayout>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </ContentPage.Content>
</ContentPage>